 <div class="container mt-5 col-6 offset-3">
  <h1>My list of Cocktails</h1>
    <p><% @cocktails.each do |cocktail| %></p>
    <%= link_to cocktail.name, cocktail_path(cocktail) %>
<% end %>
<p><%= link_to "Add a cocktail", new_cocktail_path, class: 'btn btn-primary mt-3' %></p>
